# openlatter Next.js Landing Page

This project is an engineering migration of the Open Design `index.html` export into a deployable Next.js App Router project.

## Scripts

```bash
npm install
npm run dev
npm run build
```

## Vercel

Deploy the `next-project` directory as a standard Next.js project. No custom server, local absolute resource paths, or environment variables are required.
